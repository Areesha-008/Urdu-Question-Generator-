# Urdu Question Generator — runtime dependencies
# Python >= 3.9. Install with:  pip install -r requirements.txt
# Lower bounds only; run `pip freeze > requirements.lock.txt` once training starts
# so results stay reproducible.

# --- Data pipeline (Phase 1: notebooks/data_prep.ipynb) ---
datasets>=2.14,<4
numpy>=1.24
matplotlib>=3.7

# --- Tokenizer (Phase 2: notebooks/tokenizer.ipynb) ---
sentencepiece>=0.1.99

# --- Modelling (Phase 3) ---
torch>=2.0
transformers>=4.35
accelerate>=0.25

# --- Evaluation (Phase 4) ---
# sacrebleu handles Urdu correctly (verified: BLEU/chrF give sensible scores).
sacrebleu>=2.4
# WARNING: rouge-score's default tokenizer strips every non-[a-z0-9] character,
# so it scores 0.0 on Urdu even when hypothesis == reference. Only use it with a
# custom Urdu tokenizer — see docs/repo_audit.md, finding E1.
rouge-score>=0.1.2

# Progress reporting used by the current upstream notebooks.
tqdm>=4.65.0
